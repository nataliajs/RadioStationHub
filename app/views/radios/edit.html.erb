<h1>Editing radio</h1>
 
<%= form_for :radio, url: radio_path(@radio), method: :patch do |f| %>
 
  <% if @radio.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@radio.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @radio.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :name %>
  </p>
 
  <p>
    <%= f.label :text %><br>
    <%= f.text_field :source %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
 
<% end %>
 
<%= link_to 'Back', radios_path %>
